# handoff
- Summarize the active thread context into a fresh-thread continuation prompt that is output into a fenced code block.

## How to use
1. Type `$handoff`.
2. Copy and paste the handoff block into a fresh thread.
3. Resume working in fresh thread.

## Install
`npx skills@latest add CratesSo/skills/handoff`
